/*
 * TaskAMain is the entry point for testing Task A only.
 * It is mainly used for debugging the sorting part or generating Chapter 1 output.
 */
public class TaskAMain {
    /*
     * This method runs Task A only and does not execute the graph part.
     */
    public static void main(String[] args) {
        InspectionSystem inspectionSystem = new InspectionSystem();
        inspectionSystem.runTaskAOnly();
    }
}
